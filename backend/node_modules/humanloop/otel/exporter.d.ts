import { ExportResult } from "@opentelemetry/core";
import { ReadableSpan, SpanExporter } from "@opentelemetry/sdk-trace-base";
export declare class HumanloopSpanExporter implements SpanExporter {
    private readonly hlClientBaseUrl;
    private readonly hlClientHeaders;
    private shutdownFlag;
    private readonly uploadPromises;
    constructor(options: {
        hlClientHeaders: Record<string, string>;
        hlClientBaseUrl: string;
    });
    export(spans: ReadableSpan[]): ExportResult;
    shutdown(): Promise<void>;
    forceFlush(): Promise<void>;
    private exportSpan;
    private spanToPayload;
    private hrTimeToNanoseconds;
}
