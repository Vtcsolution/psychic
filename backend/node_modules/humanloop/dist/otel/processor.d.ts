import { Context } from "@opentelemetry/api";
import { ReadableSpan, Span, SpanExporter, SpanProcessor } from "@opentelemetry/sdk-trace-node";
export declare class HumanloopSpanProcessor implements SpanProcessor {
    private spanExporter;
    constructor(exporter: SpanExporter);
    onStart(span: Span, _: Context): void;
    onEnd(span: ReadableSpan): void;
    shutdown(): Promise<void>;
    forceFlush(): Promise<void>;
}
