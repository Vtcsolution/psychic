import { Tracer } from "@opentelemetry/api";
import { HumanloopClient } from "../Client";
export declare function flowUtilityFactory<I, O>(client: HumanloopClient, opentelemetryTracer: Tracer, callable: (args: I) => O, path: string, attributes?: Record<string, unknown>): ((inputs: I) => Promise<O>) & {
    file: {
        path: string;
        type: string;
        version: {
            attributes: Record<string, unknown>;
        };
        callable: (inputs: I) => Promise<O>;
    };
};
